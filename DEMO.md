# 演示与讲解指南（DEMO）

> 面向队友 / 评审的**演示脚本**：怎么跑起来、演示讲什么、核心技术是什么、为什么这么选。
>
> 配套文档：[README.md](README.md)（产品介绍）· [DEVELOPER.md](DEVELOPER.md)（架构与工程边界）· [AGENT.md](AGENT.md)（AI 助手约束）
>
> 一句话定位：**这条链路能跑，而且我们证明了它比关键词搜索强。**

---

## 0. 五分钟跑起来

```bash
# 1) 装依赖（3 个生产依赖，几十秒）
npm install

# 2) 生成 Golden Dataset 语料（168 条内容 + ground truth 标注）
npm run gold:build

# 3) 起服务
npm run dev
# → http://localhost:3000
```

`.env.local` 里 `USE_FIXTURES=1` 时**不需要任何凭证**，全链路离线可跑。
想接真实知乎数据：把 `USE_FIXTURES` 改成 `0`，并确认 `ZHIHU_ACCESS_SECRET` 已配置。

### 三条最有说服力的命令

```bash
npm run eval          # ★ 对照实验 A/B/C，产出 eval/report.md —— 路演最重要的一页
npm run typecheck     # 类型检查，应零输出
npm run e2e           # 28 项端到端用例（零依赖，自己起 next dev）
```

`npm run eval` 会打印：

```
A 组：Top-3 有效人选率 68.5% ｜ 至少命中1位 88.9% ｜ 覆盖率 90.0%
B 组：Top-3 有效人选率 66.7% ｜ 至少命中1位 94.4% ｜ 覆盖率 90.0%
C 组：Top-3 有效人选率 85.2% ｜ 至少命中1位 100.0% ｜ 覆盖率 90.0%
```

**C 组比 A 组高约 17 个百分点，且「至少命中一位亲历者」达到 100%。**

---

## 1. 向队友展示的三种方式

### 方式一：跑对照实验（推荐，最有信息量）

```bash
npm run eval
cat eval/report.md
```

产出 `eval/report.md`，含主指标表、逐问题明细、方法学局限性声明。
`eval/raw.json` 是逐条原始数据，任何人都能复核我们的数字。

**讲解要点**：
- 三组**同一套代码**，只是配置不同（`src/domain/experiment.ts`）—— 所以对照是干净的
- ground truth 与检索语料**物理隔离**（`scripts/gold/labels.json` vs `src/back/fixtures/gold-hits.json`）—— 所以没有作弊空间
- 报告里主动写了自己的局限（合成语料、判定偏乐观、hash embedder 降级）

### 方式二：看链路在界面上跑

```bash
npm run dev
```

打开 <http://localhost:3000>，输入示例问题，能看到：

- **链路看板**：8 个步骤各自的状态、耗时、产物数量（这是「AI 在工作」的可视化）
- **推荐卡片**：为什么推荐这个人、原文证据引用、与你的相关性、**TA 不适合回答什么**
- **打分明细**：7 项权重的具体得分（`<details>` 折叠）

### 方式三：直接看产物（排查用）

每次运行都会把 8 个步骤的中间产物落盘：

```bash
ls .artifacts/                          # 每次运行一个 runId 目录
cat .artifacts/<runId>/03-recall.json   # 召回结果
cat .artifacts/<runId>/07-verified.json # 证据校验报告
```

这是「可回放、可审计」的基础 —— 任何结论都能追到中间产物，不用信我们的口头描述。

---

## 2. 演示脚本（建议按这个顺序讲，约 6 分钟）

**第 1 步 · 抛问题（30 秒）**

> 「AI 让得到答案越来越便宜。但『一个 35 岁的人该不该降薪去创业公司』这种问题，
> 真正有价值的不是答案，是**一个真的做过这个选择的人**。
> 现在的搜索能搜到 10 万篇聊这个话题的内容，但搜不到『恰好经历过的人』。」

**第 2 步 · 打开页面，提交问题（1 分钟）**

用这条（带具体处境，能触发 `human` 路由）：

```
我在一家大厂做产品经理快 5 年了，晋升通道明显变窄。现在有个 50 人的 AI 创业公司
让我去做产品负责人，但固定薪资要降 20%，给期权。我该不该去？
```

**第 3 步 · 指着链路看板讲 8 个步骤（1.5 分钟）**

重点讲三步（其它带过）：

| 步骤 | 一句话讲法 |
|---|---|
| 01 分诊 | 「先判断该不该问人。通用知识问题会被挡回去，不打扰真人 —— 这是我们和『什么都推给付费咨询』的区别」 |
| 04 经历抽取 | 「**写过 ≠ 经历过**。这一步只抽第一人称的真实决策，而且要逐字摘录原文」 |
| 07 证据校验 | 「每个推荐理由都要能在原文里**逐字定位到**。定位不到就不说 —— 这条是护城河」 |

**第 4 步 · 讲推荐卡片（1.5 分钟）**

指三个地方：

1. **原文证据引用** —— 「这不是模型编的，是原文里原样摘出来的」
2. **TA 不适合回答什么** —— 「名片型产品只讲优点，我们主动说不适合。这是诚实性设计」
3. **打分明细** —— 「7 项权重，粉丝量**不进公式**（只作为大 V 集中度这个健康指标的观测量）。
   否则结果会退化成粉丝排行榜」

**第 5 步 · 亮数据（1.5 分钟）**

```bash
npm run eval
cat eval/report.md
```

> 「A 组纯关键词 68.5%，B 组纯语义 66.7%，**C 组完整链路 85.2%**。
> 而且看这张逐问题表：q07 和 q09——也就是『IC 转管理』那类问题——
> 纯关键词组是 **0/3**，三个人一个都不对。因为职场讨论里『管理岗』这个词太泛，
> 搜出来的全是方法论文章。加了经历抽取和证据校验之后，2/3 和 3/3。」

---

## 3. 核心技术实现

### 3.1 架构：端口-适配器 + 确定性工作流

```
src/
├── domain/      契约冻结层   types.ts（业务类型）· experiment.ts（A/B/C 配置）
├── framework/   通用地基     pipeline.ts（引擎）· bm25/rrf/vector（检索）· cache/quota/timeout
├── steps/       业务骨干     8 个步骤，一步一个文件
├── adapters/    适配器       zhihu-http-source / source-fixture / llm / 装配根 index.ts
└── app/         薄 IO 层     Route Handlers + 页面
```

**三条硬边界**（`DEVELOPER.md` 有完整说明）：

1. `framework/` 里**不出现任何业务词** —— 它是通用地基，不是业务代码
2. `steps/` **不 import 任何具体实现类** —— 只依赖 `framework/ports.ts` 的接口
3. `adapters/index.ts` 是**唯一**知道所有实现的地方（装配根）

### 3.2 为什么是工作流，不是 Agent Runtime

这条链路的每一步输入输出**都可预先定义**，没有任何一步需要模型自主决定「下一步做什么」：

```
01 分诊 → 02 结构化 → 03 召回 → 04 经历抽取 → 05 聚合 →
06 重排 → 07 证据校验 → 08 解释生成
```

需要「自主性」的只有第一步的三路分支，而那是**分类结果**，不是决策。
用 Agent Runtime 的代价是：不确定的步数、不确定的顺序、无法预先算延迟预算。
我们需要的是**确定性**——因为要跑 60 次对照实验比较指标。

**但引擎的设计留了换实现的余地**：将来想换成 LangGraph，只需替换 `runPipeline` 这个
driver，步骤本身不用动。

### 3.3 混合召回（第三步的核心）

三路融合，这是「C 组为什么更强」的一半原因：

| 路 | 实现 | 擅长 |
|---|---|---|
| 关键词 | 知乎 search 接口多路查询 + 命中词加权 | 术语精确命中（「期权 成熟期」） |
| 语义 | 向量 cosine（见下方选型） | 跨措辞（用户说「画饼」，作者写「期望值几乎可以忽略」） |
| 融合 | **RRF**：`score = Σ 1/(k + rank)`，k=60 | 只看名次，免疫量纲差异 |

**为什么用 RRF 而不是加权求和**：BM25 分值域是 0~20+，cosine 是 0~1，
加权求和就得先归一化，而归一化方式本身又变成拍脑袋的超参。
RRF 只用名次，对某一路的异常高分不敏感 —— 而且「被两路都排第 5」
比「只被一路排第 1」更吃香，这正是我们要的互补性。

### 3.4 「写过 ≠ 经历过」在代码里的落地

这是产品最硬的一条约束，落在两处：

**抽取预算分配（`04-extract.ts`）** —— 全项目最贵的一步（每条内容一次 LLM 调用），
但召回是按「与问题的相关性」排的，不是按「有没有亲身经历」排的。

> 实测踩过：真实数据一次运行召回 16 条，相关性最高的前 2 条是政策解读和行业综述
> （正文里连一个「我」都没有），而真正带第一人称经历的那条排在第 15。
> 把预算花在前几名上 = 用最贵的算力处理最没用的内容，那次抽出 **0 条**经历。

所以先用**便宜的正则**判断哪条值得花**昂贵的模型调用**：

```ts
scoreForExtraction(hit) =
  STRONG_FIRST_PERSON 命中 +3（我当时 / 我最后 / 我选择…）
  WEAK_NARRATIVE 命中 +1（去年 / 当时 / 复盘 / 踩坑…），上限 14
  GUIDE_LIKE 标题 -4（指南 / 教程 / 盘点 / 榜单…）
  正文 < 120 字 -3，≥ 300 字 +1
```

**证据逐字回溯（`07-verify.ts`）** —— 确定性判断，**不是再问一次模型**：

```ts
quoteIsTraceable(quote, sourceText)  // 去空白标点后做 substring 包含
```

用模型校验模型等于没校验。而且**一条都没过的候选人直接剔除**（宁缺毋滥）。

### 3.5 每个 LLM 调用都有确定性降级

```ts
llmOrFallback(ctx, req, fallback)   // LLM 挂了就走 fallback，链路不断
```

理由有三个：额度有限（100 次/天）；现场网络可能不通；
而「信心护栏」本来就要求低证据时宁缺毋滥，而不是编。

这也是为什么 `npm run eval` 用 `LLM_PROVIDER=none` 也能跑出有意义的对照 ——
三组都在同一个降级基线上比较，**比较的是算法结构，不是模型随机性**。

---

## 4. 技术选型与理由

| 决策点 | 选择 | 理由 | 放弃了什么 |
|---|---|---|---|
| 前端框架 | **Next.js 15 App Router** + React 19 + Tailwind 3 | 页面壳、Route Handler、部署一体化；与队友的 zhihu-circle 同源 | — |
| 后端 | **Next.js Route Handlers**（`runtime='nodejs'`） | 不需要独立后端服务，一个进程搞定；hackathon 阶段够用 | 高并发时的横向扩展能力 |
| 生产依赖 | **只有 3 个包**（next / react / react-dom） | 攻击面最小、`npm audit` = 0、CI 最快 | 要自己写一些工具（BM25/RRF/缓存） |
| 工作流 | **自研确定性引擎**（约 270 行） | 步数固定、顺序固定 → 延迟可预算、可缓存、可对照 | Agent Runtime 的灵活性 |
| 检索融合 | **自研简化 BM25 + RRF** | 量级只要几百条，`minisearch`/向量库都是复杂度浪费 | 现成库的成熟度 |
| 向量存储 | **不上向量库**，落盘 JSON + 内存 cosine | 1000 条 × 1024 维 ≈ 400 万次乘加 = Node 里毫秒级 | 万级以上语料要换成 `sqlite-vec` |
| Embedding | **默认 hash 降级**，可显式配 `EMBEDDING_*` | 「OpenAI 兼容的 chat 端点」≠「有 embeddings 端点」，自动复用会 404 崩链路 | 默认没有真语义能力（已如实声明） |
| 缓存 | **自研磁盘 KV**（sha256 key + TTL） | 保护额度 + 演示不依赖实时网络 + 让对照实验可复现 | 现成 Redis 的性能 |
| 工作流缓存 | **`pipeline.version` 拼进所有 key** | 改逻辑后 bump version 即可整体失效 | — |
| 会话 | **AES-256-GCM 加密的 HttpOnly Cookie** | token 不进浏览器、不进前端日志；服务端无状态 | 无法主动吊销（MVP 可接受） |
| 证据校验 | **确定性 substring 回溯** | 可解释、零成本、不会被模型骗 | 换个说法就过不了（可升级为语义级 + 确定性兜底） |

### 一个必须讲清的选择：为什么不上向量数据库

语料是几百条量级。向量化一次 → 落盘 JSON → 内存算 cosine，是**毫秒级**的事。
上 Chroma / Qdrant 会引入一个需要运维的进程、一个持久化目录、
一套一致性语义 —— 全部是纯粹的复杂度浪费。

**什么时候才需要**：超过 1 万条，或需要增量更新。那时用 `sqlite-vec`（单文件、零服务）
比 Chroma 更省事。这个判断写进了 `internal/STATUS.md`，是团队共识。

---

## 5. 指标定义（讲数据时必须能解释清楚）

| 指标 | 定义 | 怎么读 |
|---|---|---|
| **Top-3 有效人选率** | 推荐的前 3 人里，「确实第一人称写过该主题」的比例 | **主指标**，越高越好 |
| 至少命中 1 位 | Top-3 里有亲历者的比例 | 更抗噪声的二值指标 |
| 产出覆盖率 | 能为多少问题产出推荐 | Triage 判 `ai`/`content` 的问题会走「别问人」路径，**不算失败** |
| 证据覆盖率 | 外显理由能逐字回溯的比例 | 只有 C 组做校验，所以 A/B 组恒为 0 |
| 大 V 集中度 | 推荐结果被高赞用户占据的程度 | **健康指标**，越低越好（粉丝量不进打分公式） |
| P95 延迟 | 95 分位响应时间 | 受 `maxDuration=60` 约束 |

### 已知局限（主动声明，别等评审问）

1. **语料是合成的**（168 条，`scripts/gold/corpus.mjs`）——
   每条的 ground truth 在生成时就写好了。这验证了「方法在受控条件下有效」，
   **不能替代真实数据结论**。
2. **「有效」的判定是上界** —— 只判断「有没有写过该主题」，不判断「写的内容是否
   真的能回答这个具体问题」。所以数字偏乐观。
3. **B 组是弱基线** —— 默认 embedder 是 hash 投影（词元重合度），不是真语义模型。
   所以 B vs A 的差异不代表语义召回的真实能力。C 组的增益主要来自
   「经历抽取 + 证据校验」，这部分不依赖 embedding 质量。
4. **真实数据部分另有说明** —— `scripts/harvest.mjs` 能抓真实知乎内容，
   但那部分**没有 ground truth**，只能人工抽查。两者在报告里分开陈述。

> 这套局限声明是刻意的：**评审最可能质疑的不是代码，是「证明不了」。**
> 主动把边界说清楚，比被人问出来要好得多。

---

## 6. 复现与健壮性

```bash
npm run typecheck      # tsc --noEmit，应零输出
npm run build          # 生产构建
npm run e2e            # 28 项端到端用例（含证据可回溯不变式）
npm run e2e:llm        # 用真实 LLM 跑（慢，且会消耗额度）
npm run publish:check  # 发布前自检：真实密钥反查 + 投毒验证
```

### 三条被「实测教训」逼出来的不变式

1. **缓存 key 里必须带 `pipeline.version`** —— 否则改逻辑后拿到旧结果，
   而且每一步都命中缓存、trace 全绿，**极难排查**。
2. **所有出站 fetch 必须有超时** —— 没有超时的外部调用 = 没有降级。
   实测踩过：一次抽取调用挂死 300 秒，请求一直不返回，而降级逻辑
   只在 promise reject 时才生效。
3. **缓存与额度护栏必须是模块级单例** —— serverless 上磁盘只读会退回进程内计数，
   每次请求新建实例的话计数永远从 0 开始，护栏等于失效。

---

## 7. 演示前的检查清单

- [ ] `npm install` 完成，`npm run typecheck` 零输出
- [ ] `npm run gold:build` 跑过（168 条语料已生成）
- [ ] `npm run eval` 跑过一遍，`eval/report.md` 数字已确认
- [ ] `npm run dev` 起得来，示例问题能返回推荐卡片
- [ ] **跑一遍要现场演示的那几个问题**，让 `.cache/` 生效（现场完全不依赖实时网络）
- [ ] `USE_FIXTURES=1`（演示用 fixture，稳定可复现）
- [ ] 如果现场网络好，可以临时切 `USE_FIXTURES=0` 展示真实知乎数据
